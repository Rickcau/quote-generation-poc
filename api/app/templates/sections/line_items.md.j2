{# section: line_items #}
## Services

| Category | Description | Qty | Unit | Unit Price | Total |
|----------|-------------|-----|------|-----------|-------|
{%- for item in quote.line_items %}
| {{ item.service_category }} | {{ item.service_description }} | {{ item.quantity }} | {{ item.unit }} | ${{ "%.2f"|format(item.unit_price) }} | ${{ "%.2f"|format(item.line_total) }} |
{%- endfor %}

**Subtotal:** ${{ "%.2f"|format(quote.subtotal) }}
**Tax ({{ "%.2f"|format(quote.tax_rate * 100) }}%):** ${{ "%.2f"|format(quote.tax_amount) }}
**Total:** ${{ "%.2f"|format(quote.total) }}
